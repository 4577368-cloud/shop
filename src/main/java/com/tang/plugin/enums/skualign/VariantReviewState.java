package com.tang.plugin.enums.skualign;

/** Review layer state — not equivalent to fulfillable binding. */
public enum VariantReviewState {
    SUGGESTED,
    UNMAPPED,
    NO_SOURCE,
    RESOLVED
}
